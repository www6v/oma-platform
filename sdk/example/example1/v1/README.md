# Deprecated — pre-parity workaround copy

This directory holds the **legacy** monolithic `data_analyst_agent.py` from before
P0 cookbook alignment (disk fallbacks, poll-based events, etc.).

**Use instead:**

- Launcher: `../data_analyst_agent.py`
- Logic: `../data_analyst_agent_main.py`

Kept for diff/reference only; not maintained for parity.
